require "rspec"
require "spec_helper"

describe "Spells Controller" do
  context "Status Codes" do
    xit "returns a 300 when redirecting from root" do
      expect(last_response.status).to eq 302
    end

    xit "returns a 200 when something is good" do

    end

    xit "returns a 400 when input is invalid" do
      
    end
  end
end
